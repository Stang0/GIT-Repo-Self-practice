// นำเข้าโมดูล mathUtils
const math = require('./mathUtils.js');

const num1 = 10;
const num2 = 5;

console.log(`${num1} + ${num2} = ${math.add(num1, num2)}`); 
console.log(`${num1} - ${num2} = ${math.subtract(num1, num2)}`); 